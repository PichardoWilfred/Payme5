import { ImgDropDirective } from './img-drop.directive';

describe('ImgDropDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgDropDirective();
    expect(directive).toBeTruthy();
  });
});
